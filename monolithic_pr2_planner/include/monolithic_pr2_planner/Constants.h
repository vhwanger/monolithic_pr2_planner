#pragma once
enum BodyDOF {X, Y, Z, THETA};

enum Joints { 
    SHOULDER_PAN,
    SHOULDER_LIFT,
    UPPER_ARM_ROLL,
    ELBOW_FLEX,
    FOREARM_ROLL,
    WRIST_FLEX,
    WRIST_ROLL
};

enum ArmSide {
    LEFT,
    RIGHT
};
